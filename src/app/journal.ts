export interface Journal {
    abstract: string,
    authors: [string],
    conclusion: string,
    createdAt: string,
    description: string,
    references: [string],
    title: string,
    updatedAt: string,
    username: string,
    _v:number,
    _id: string
}
